pipeline{
     agent {
        docker {image 'python:latest'}
    }
    stages{
        stage('run'){
            steps{
                sh 'python3 pie.py > some.jar'
            }
        }
    }
    post{
         success{
            archiveArtifacts artifacts: '**/*.jar'
        }
    }
   
}
